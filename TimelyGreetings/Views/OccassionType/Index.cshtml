@model IEnumerable<TimelyGreetings.Repository.OccassionType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OccassionTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OccassionTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OccassionTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OccassionTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OccassionTypeID })
        </td>
    </tr>
}

</table>
